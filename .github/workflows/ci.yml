# ═══════════════════════════════════════════════════════════════════════════
# Continuous Integration Workflow
# Runs on pull requests and pushes to main/develop
# Validates code quality, runs tests, and validates Helm charts
# ═══════════════════════════════════════════════════════════════════════════

name: CI - Build & Test

on:
  pull_request:
    branches: [main, develop]
    types: [opened, synchronize, reopened]
  push:
    branches: [main, develop]
  workflow_dispatch:

jobs:
  # ─────────────────────────────────────────────────────────────────────────
  # Lint and Type Check
  # ─────────────────────────────────────────────────────────────────────────
  lint:
    name: Lint & Type Check
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'

      - name: Install Dependencies
        run: npm ci

      - name: TypeScript Type Check
        run: npm run build

      - name: Check for Build Errors
        run: |
          if [ -d "dist" ]; then
            echo "✅ TypeScript build successful"
          else
            echo "❌ Build failed - dist directory not created"
            exit 1
          fi

  # ─────────────────────────────────────────────────────────────────────────
  # Validate Helm Chart
  # ─────────────────────────────────────────────────────────────────────────
  helm-validate:
    name: Validate Helm Chart
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Setup Helm
        uses: azure/setup-helm@v3
        with:
          version: 'v3.14.0'
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Login to Docker Hub OCI Registry
        continue-on-error: true
        run: |
          if [ -n "$DOCKER_HUB_LOGIN" ] && [ -n "$DOCKER_HUB_PWD" ]; then
            echo "$DOCKER_HUB_PWD" | helm registry login -u "$DOCKER_HUB_LOGIN" --password-stdin docker.io
            echo "✅ Logged in to Docker Hub OCI registry"
          else
            echo "⚠️ Docker Hub credentials not available, skipping OCI login"
          fi
        env:
          DOCKER_HUB_LOGIN: ${{ secrets.DOCKER_HUB_LOGIN }}
          DOCKER_HUB_PWD: ${{ secrets.DOCKER_HUB_PWD }}

      - name: Update Helm Dependencies
        continue-on-error: true
        run: |
          helm dependency update ./charts || echo "⚠️ Could not fetch all dependencies (OCI registry login may be required)"

      - name: Lint Helm Chart
        run: helm lint ./charts

      - name: Template Helm Chart (dry-run)
        run: |
          # Check if dependencies were fetched successfully
          if [ -d "./charts/charts" ] && [ "$(ls -A ./charts/charts 2>/dev/null)" ]; then
            echo "📦 Dependencies available, running full template test"
            helm template concieragent ./charts \
              --namespace demos \
              --set global.domain=test.example.com \
              --set secrets.openaiApiKey=test-key \
              --set secrets.serpapiKey=test-key \
              --set secrets.openweatherApiKey=test-key \
              --debug > /dev/null
            echo "✅ Helm template generation successful"
          else
            echo "⚠️ Dependencies not available, running template without subchart"
            # Remove dependency requirement temporarily for CI
            sed -i '/^dependencies:/,/condition:/d' ./charts/Chart.yaml
            helm template concieragent ./charts \
              --namespace demos \
              --set global.domain=test.example.com \
              --set secrets.openaiApiKey=test-key \
              --set secrets.serpapiKey=test-key \
              --set secrets.openweatherApiKey=test-key \
              --set vs-agent-chart.enabled=false \
              --debug > /dev/null
            echo "✅ Helm template generation successful (without vs-agent-chart dependency)"
          fi

  # ─────────────────────────────────────────────────────────────────────────
  # Docker Build Test (no push)
  # ─────────────────────────────────────────────────────────────────────────
  docker-build:
    name: Docker Build Test
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build Docker Image (test only)
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./Dockerfile
          push: false
          tags: concieragent:test
          cache-from: type=gha
          cache-to: type=gha,mode=max

  # ─────────────────────────────────────────────────────────────────────────
  # Security Scan
  # ─────────────────────────────────────────────────────────────────────────
  security:
    name: Security Scan
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Run Trivy vulnerability scanner
        uses: aquasecurity/trivy-action@master
        with:
          scan-type: 'fs'
          scan-ref: '.'
          format: 'table'
          exit-code: '0'  # Don't fail on vulnerabilities for now
          ignore-unfixed: true
          severity: 'CRITICAL,HIGH'

  # ─────────────────────────────────────────────────────────────────────────
  # Summary Job (depends on all above)
  # ─────────────────────────────────────────────────────────────────────────
  ci-success:
    name: CI Success
    runs-on: ubuntu-latest
    needs: [lint, helm-validate, docker-build, security]
    if: always()
    
    steps:
      - name: Check all jobs
        run: |
          if [ "${{ needs.lint.result }}" != "success" ] || \
             [ "${{ needs.helm-validate.result }}" != "success" ] || \
             [ "${{ needs.docker-build.result }}" != "success" ]; then
            echo "❌ One or more CI jobs failed"
            exit 1
          fi
          echo "✅ All CI jobs passed!"
